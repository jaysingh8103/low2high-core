# Authentication

- Standard JWT (JSON Web Tokens) for user/agency authentication.
- API Keys for programmatic access (e.g., CRMs or webhooks calling our platform).
- Bearer token format: `Authorization: Bearer <token>`
