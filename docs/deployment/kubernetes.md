# Kubernetes

- Helm charts for defining deployments, services, and ingress.
- Separate deployments for API, Manager Agent, and scraping workers.
- HPA (Horizontal Pod Autoscaler) enabled for worker nodes based on CPU/Queue length.
