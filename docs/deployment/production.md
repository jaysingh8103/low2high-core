# Production Checklist

- [ ] DB connection pooling configured (e.g., PgBouncer).
- [ ] Redis memory limits and eviction policies set.
- [ ] Secrets managed via external vaults (AWS Secrets Manager / Hashicorp Vault).
- [ ] Centralized logging and alerting active.
- [ ] SSL certificates configured via Let's Encrypt / API Gateway.
