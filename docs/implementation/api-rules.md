# API Rules

- Use RESTful principles.
- Use standard HTTP status codes (200, 201, 400, 401, 404, 500).
- All requests and responses must use JSON.
- Implement rate limiting on public search endpoints.
