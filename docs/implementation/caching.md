# Caching

- Cache Google API responses to save costs.
- Cache public website DOMs if auditing multiple pages on the same domain within 24 hours.
- Use Redis as the primary caching layer with TTLs configured based on data volatility.
